<turbo-frame id="diaryEntry_{{ diaryEntry.id }}">
{{ include('mappable/edit_frame.html.twig', {
    object_name: 'diaryEntry',
    object_name_plural: 'diaryEntries',
    mappable: diaryEntry
}) }}
</turbo-frame>
