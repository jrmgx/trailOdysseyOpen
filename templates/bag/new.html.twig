{% extends 'home/index.html.twig' %}

{% block main %}
    <div class="row">
        {{ include('common/_header_page.html.twig', {
            header_title: 'title.your_bags'|trans,
            header_back_title: 'action.back'|trans,
            header_back_href: path('bag_index', { trip: trip.id }),
        }) }}
        <div class="col-12">
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.description) }}
            {{ form_row(form.weight) }}
            <button class="btn btn-primary button-inline btn-block">{{ 'action.save'|trans }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
